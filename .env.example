# PostgreSQL connection (Neon.tech free tier)
DATABASE_URL=postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/instascope?sslmode=require

# Flask
SECRET_KEY=change-me-to-random-string

# Instagram session (optional — can also set via UI)
IG_SESSION_ID=
